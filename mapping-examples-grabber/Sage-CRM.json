{
  "sage": {
    "mappings": {
      "lead": {
        "personsalutation": "{{ gender == -1? ('') : (gender == 0? ('Herr') : ('Frau')) }}",
        "personfirstname": "{{ firstName }}",
        "personlastname": "{{ lastName }}",
        "companywebsite": "{{ website || _delete  }}",
        "persontitle": "{{ position || _delete  }}",
        "personemail": "{{ email }}",
        "personphonecountrycode": "{{ phone_countrycode || _delete  }}",
        "personphoneareacode": "{{ phone_areacode || _delete  }}",
        "personphonenumber": "{{ phone_number || _delete  }}",
        "personfaxcountrycode": "{{ fax_countrycode || _delete }}",
        "personfaxareacode": "{{ fax_areacode || _delete }}",
        "personfaxnumber": "{{ fax_number || _delete  }}",
        "companyaddress1": "{{ street || _delete  }}",
        "companycity": "{{ city || _delete  }}",
        "companypostcode": "{{ zip || _delete  }}",
        "companystate": "{{ state || _delete  }}",
        "companycountry": "{{ country || _delete  }}",
        "companyname": "{{ organization }}",
        "source": "{{ }}",
        "status": "{{ }}",
        "description": "{{ }}",
        "details": "{{note || _delete}}"
      },
      "contact": {
        "salutation": "{{ gender == -1? ('') : (gender == 0? ('Herr') : ('Frau')) }}",
        "firstname": "{{ firstName }}",
        "lastname": "{{ lastName }}",
        "title": "{{ title  || _delete }}",
        "titlecode": "{{ position || _delete }}",
        "website": "{{ website || _delete }}",
        "address": {
          "entityname": "address",
          "records": [
            {
              "address1": "{{ street || _delete }}",
              "city": "{{ city || _delete }}",
              "state": "{{ state || _delete }}",
              "postcode": "{{ zip || _delete }}",
              "country": "{{ country || _delete }}",
              "type": "Business"
            }
          ]
        },
        "email": {
          "entityname": "email",
          "records": [
            {
              "emailaddress": "{{ email || _delete }}",
              "type": "business"
            }
          ]
        },
        "phone": {
          "entityname": "phone",
          "records": [
            {
              "countrycode": "{{ phone_countrycode || _delete }}",
              "areacode": "{{ phone_areacode || _delete }}",
              "number": "{{ phone_number || _delete }}",
              "type": "business"
            },
            {
              "countrycode": "{{ fax_countrycode || _delete }}",
              "areacode": "{{ fax_areacode || _delete }}",
              "number": "{{ fax_number || _delete }}",
              "type": "fax"
            },
            {
              "countrycode": "{{ mobile_countrycode || _delete }}",
              "areacode": "{{ mobile_areacode || _delete }}",
              "number": "{{ mobile_number || _delete }}",
              "type": "mobile"
            }
          ]
        }
      },
      "company": {
        "name": "{{ organization }}",
        "website": "{{ website || _delete }}",
        "status": "{{ status || _delete }}",
        "primaryuserid": "{{ userId || _delete }}",
        "address": {
          "entityname": "address",
          "records": [
            {
              "address1": "{{ street || _delete }}",
              "city": "{{ city || _delete }}",
              "state": "{{ state || _delete }}",
              "postcode": "{{ zip || _delete }}",
              "country": "{{ country || _delete }}",
              "type": "business"
            }
          ]
        },
        "email": {
          "entityname": "email",
          "records": [
            {
              "emailaddress": "{{ email || _delete }}",
              "type": "business"
            }
          ]
        },
        "phone": {
          "entityname": "phone",
          "records": [
            {
              "countrycode": "{{ phone_countrycode || _delete }}",
              "areacode": "{{ phone_areacode || _delete }}",
              "number": "{{ phone_number || _delete }}",
              "type": "business"
            },
            {
              "countrycode": "{{ fax_countrycode || _delete }}",
              "areacode": "{{ fax_areacode || _delete }}",
              "number": "{{ fax_number || _delete }}",
              "type": "fax"
            },
            {
              "countrycode": "{{ mobile_countrycode || _delete }}",
              "areacode": "{{ mobile_areacode || _delete }}",
              "number": "{{ mobile_number || _delete }}",
              "type": "mobile"
            }
          ]
        }
      },
      "account": {
        "name": "{{ organization }}",
        "primaryuserid": "{{ userId }}",
        "website": "{{ website }}"
      }
    }
  }
}